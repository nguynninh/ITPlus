namespace MidtermExam2.exercise2;

/** Bài 2: 20 điểm
 * Tạo 1 class Animal chứa các thông tin: ID(int), tên (string), ăn thịt hay không (bool), tốc độ chạy (float).
 * Tất cả các thông tin trừ name đều không public, và chỉ set qua constructor.
 * Tạo constructor cho Animal, có tham số và không có tham số.
 */
public class TestAnimal
{
 
}